<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahavir_agro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sample"></activity>
        <activity android:name=".report" />
        <activity android:name=".outData" />
        <activity android:name=".outWard1" />
        <activity android:name=".new_entry2" />
        <activity android:name=".CustNameSpinner" />
        <activity android:name=".CustNameData" />
        <activity android:name=".ViewData" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>